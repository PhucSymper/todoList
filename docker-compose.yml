version: "3"
services:
  web:
    container_name: echo_app
    build: ./
    command: go run server.go
    volumes:
      - .:/echo_app
    ports:
      - "1234:1234"
    networks:
      - customnetwork
networks:
  customnetwork:
    driver: bridge
